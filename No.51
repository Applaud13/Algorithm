function solution(food) {
    var answer = '';
    let k = 0;
    for(let i=1; i<food.length; i++){
        for(let x=0; x<food[i]/2-0.5; x++){
            answer += i
        }
    }
    return answer + `0` + answer.split(``).reverse().join(``);
}
