function solution(s) {
    var answer = '';
    let string = [];
    for (let i = 0; i < s.length; i++) {
        string[i] = s[i];
    }
    string.sort();
    string.reverse();
    for (let i = 0; i < s.length; i++) {
        answer += string[i];
    }
    return answer;
}
